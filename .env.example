# Backend API base URL.
# Set per-environment in Vercel (Settings -> Environment Variables):
#   Production scope -> production Railway URL
#   Preview scope    -> staging Railway URL (used by develop + feature previews)
# Locally this points at the backend dev server.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
